<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>question</title>
  </head>
  <body>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../../Quiz-App-JavaScript-main/css/style.css" />
    <!-- FontAweome CDN Link for Icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />

    <!-- Quiz Box -->
    <% if(s == true){ %> 
    <div class="quiz_box activeQuiz">
      <header>
        <div class="title">Homework</div>
     
      </header>
      <section>
        <div class="que_text"><span><%=quiz['homeId']['quiz'][random]['question'] %></span></div>
        <div class="option_list">
          <% for(i of quiz['homeId']['quiz'][random]['answer'][0]){ %> 
           <form method="post">
            <% if(i !="") {%> 
            <div class="option " >
             
              <span><%=i %></span>
              <input
                type="radio"
                name="answer"
                value="<%=i%>" 
              />
              </div>
              <% } %> 
              <% } %>

         
        </div>
      </section>

      <!-- footer of Quiz Box -->
      <footer>
        <div class="total_que">
          <span
            ><p><%=index %> </p>
            of
            <p>40 </p>
            Questions</span
          >
        </div>
       
        <% if(index == 39) {%> 
          <button class="next_btn show">Finish</button>
          <% }else{ %> 
            <button class="next_btn show">Next</button>
          <% } %> 
        </form>
      </footer>
    </div>
    <% } else { %>
      <h1>no quiz</h1>
  <% } %>

    <!-- Code injected-->
  </body>
</html>
